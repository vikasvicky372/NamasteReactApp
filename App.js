const heading = React.createElement("h1", {id:"heading"}, "Hello Vikky from React");

//creating root

const root = ReactDOM.createRoot(document.getElementById("root"));

//rendering root

root.render(heading);